pub func:main = int32() {
    wild int64->:p1 = alloc(32i64) => int64->;
    wild int64->:p2 = alloc(32i64) => int64->;
    
    int64:addr1 = @cast_unchecked<int64>(p1);
    int64:addr2 = @cast_unchecked<int64>(p2);
    
    if (addr1 == addr2) {
        exit(1i32);
    }
    exit(0i32);
};

func:failsafe = int32(tbb32:err_code) {
    exit(err_code => int32);
};
